var value = 42;

if (value >= 53){
	value = value + 42;
}else{
	value = value - 13;
};

var newString = "11";
value = (value + newString);

var newArray = [];

for (var i = 0; i < value.length; i++) {
	newArray = value.charAt[i];
};

newArray.shift();
newArray.pop();

var backwards = 0;

for (var i = 2; i >= 0; i--) {
	backwards = (backwards + [i]);
};

value = parseInt(value);
backwards = parseInt(backwards);
